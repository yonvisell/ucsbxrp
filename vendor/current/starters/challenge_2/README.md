# Challenge 2: Turn and Return

Students add differential-drive inverse kinematics and odometry. The project
starts with supplied components; change one flag in `course_setup.py` after the
corresponding student component passes its software tests.
