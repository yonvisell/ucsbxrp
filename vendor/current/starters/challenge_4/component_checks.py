"""Select the Challenge 4 components checked by the IDE.

Choose Test components while implementing a student class. These checks use
MicroPython but do not start the virtual or physical robot.
"""

# Normal use: do not edit this file. Implement a method in one of the student
# modules, choose Test components, and read that class's line in Program output.
#
# Example: after implementing GridPlanner.plan(), run the checks. PASS means the
# stated behavior passed; PENDING means a method is not implemented yet; FAIL
# names behavior to inspect. A later component may remain PENDING while you work
# on the current one.

from differential_drive import DifferentialDrive
from grid_planner import GridPlanner
from navigation_controller import NavigationController
from odometry import Odometry
from sensor_model import SensorModel
from wheel_speed_controller import WheelSpeedController

from ucsb_xrp.component_checks import run_component_checks


run_component_checks(
    sensor_model=SensorModel,
    wheel_speed_controller=WheelSpeedController,
    differential_drive=DifferentialDrive,
    odometry=Odometry,
    navigation_controller=NavigationController,
    grid_planner=GridPlanner,
)
