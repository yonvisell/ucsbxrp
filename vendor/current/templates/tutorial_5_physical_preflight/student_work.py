# Summarize RobotState samples collected during Tutorial 5.


def preflight_report(states: object) -> dict:
    # Reject an empty collection, then use one loop and named accumulators.
    # Treat range None as unavailable, not as zero millimeters.
    raise NotImplementedError("complete preflight_report")
